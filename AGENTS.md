# 프로젝트 규칙 (AGENTS.md)

> 이 파일은 에이전트(커서/클로드)가 자동으로 참고하는 "이 레포의 규칙집"입니다.
> `< >` 로 표시된 빈칸을 실제 정보로 채우세요. 모르면 에이전트에게 물어서 채우세요.

## 이 프로젝트는
- <한 줄 설명: 무엇을 하는 서비스/도구인가>
- 주요 사용자/목적: <누가, 왜 쓰는가>

## 기술 스택
- 언어: <예: TypeScript / Python / Go>
- 프레임워크: <예: Next.js / FastAPI>
- DB / 인프라: <예: PostgreSQL, Redis, Docker>
- 패키지 매니저: <예: npm / pnpm / uv / poetry>

## 폴더 구조 (핵심만)
- `<폴더>/` — <역할>
- `<폴더>/` — <역할>
- 진입점(main): `<경로>`

## 자주 쓰는 명령어  (※ 반드시 실제로 동작하는지 확인 후 기입)
- 설치:   `<예: npm install>`
- 실행:   `<예: npm run dev>`
- 테스트: `<예: npm test>`
- 타입/빌드: `<예: npm run build / tsc>`
- 린트:   `<예: npm run lint>`

## 코딩 규칙
- 네이밍: <예: 함수 camelCase, 컴포넌트 PascalCase>
- 포맷/스타일: <예: prettier 설정을 따른다>
- 파일 배치: <예: 새 컴포넌트는 src/components 아래>

## 절대 하지 말 것 (금지사항)
- 확신 없이 대량 파일을 한꺼번에 수정하지 않는다.
- 테스트/빌드를 깨뜨린 채로 작업을 끝내지 않는다.
- 시크릿(.env, 키파일)을 커밋하지 않는다.
- <이 프로젝트 특유의 금지사항을 여기 계속 추가>

## 작업 방식 (에이전트가 지켜야 할 루프)
1. 큰 작업은 작은 단위로 쪼개서 하나씩 처리한다.
2. 변경한 뒤에는 **반드시** 테스트/빌드/실행으로 검증하고 결과를 보고한다.
3. 확신이 없으면 코드를 고치기 전에 먼저 질문한다.
4. 같은 문제를 3번 시도해도 실패하면 멈추고, 상황을 정리해서 사람에게 보고한다.
5. 무엇을 왜 바꿨는지 간단히 설명한다.
